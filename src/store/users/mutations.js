export function setUsers(state, users) {
    state.users = users;
}

export function SET_SEARCH(state, users) {
    state.search = users;
}

export function setPagination(state, pagination) {
    state.pagination = pagination;
}

export function setUser(state, user) {
    state.user = user;
}
